[project]
name = "echo-app"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "ociapp",
    "pydantic>=2.11.9",
]

[tool.ociapp-build]
entrypoint = "echo_app.main:app"
system-packages = ["git"]
