<!-- pragma: no ai -->
# Root README API Usage Section

## Summary

- Expand [README.md](/home/elias/Codespace/ociapp/README.md) with a short
  `API Usage` section placed before `Development`.
- Keep the section intentionally selective: document the existing happy path
  across the public surface, not every exported helper or internal protocol
  detail.

## Implementation Changes

- Add a brief framing sentence that presents OCIApp as a 3-step flow: define
  an `Application`, build a `.ociapp` artifact, execute it with `Runtime`.

### Define an application

- Add one compact Python snippet based on
  [main.py](example/echo-app/src/echo_app/main.py)
  that:
  - shows `BaseModel` request/response models
  - shows `Application[Request, Response]`
  - ends with `app = EchoApplication()`
- Include one sentence explaining that the exported `app` object is resolved
  from an import path like `echo_app.main:app`.

### Build and execute an artifact

- Add a minimal `pyproject.toml` fragment showing `[tool.ociapp-build]` with
  `entrypoint = "echo_app.main:app"`.
- Add a shell block using:

```bash
uv run --no-sync ociapp-build example/echo-app --output-dir dist
```

- Add a short runtime snippet based on
  [runtime_demo.py](example/runtime_demo.py)
  showing `async with Runtime(...)` and
  `await runtime.execute(Path("dist/echo-app-0.1.0.ociapp"), {"value": "hello"})`.

- Add one concise note explaining the boundary:
  - inside the app, requests and responses are validated Pydantic models
  - on the host side, `Runtime.execute` sends and receives
    `dict[str, object]` payloads
- End the section with one short “see the examples for more detail” line
  instead of expanding into protocol internals or exhaustive API listings.

## Public Interfaces

- Document existing interfaces only; do not change code or package APIs.
- Name `ociapp.Application`, the `pkg.module:attribute` entrypoint contract
  used by `ociapp-build` and `ociapp serve`, and
  `ociapp_runtime.Runtime.execute`.
- Mention `ociapp-build` as the packaging CLI, but do not enumerate lower-level
  build helpers, engine adapters, protocol helpers, or the full error
  taxonomy.

## Test Plan

- Verify every command and identifier against the current repo before merging
  the README change:
  - `uv run --no-sync ociapp --help`
  - `uv run --no-sync ociapp-build --help`
  - confirm the example artifact name resolves to `echo-app-0.1.0.ociapp`
- Review the README for brevity:
  - exactly two usage snippets
  - no low-level UDS/protocol walkthrough
  - no duplication of package-level API inventories
- When implementing the docs change, run the repo validations required in this
  workspace:
  - `uv run --no-sync pytest`
  - `uv run --no-sync prek run -a`

## Assumptions and Defaults

- The target is the root [README.md](/home/elias/Codespace/ociapp/README.md),
  not the package READMEs.
- The audience is a first-time user evaluating OCIApp end-to-end.
- Managed build mode is the only build mode worth surfacing in the root README;
  custom `Containerfile` support stays out of scope for this section.
