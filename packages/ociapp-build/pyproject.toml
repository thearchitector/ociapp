[project]
name = "ociapp-build"
version = "0.1.0"
description = "OCIApp build tool"
readme = "README.md"
license = "BSD-3-Clause-Clear"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ociapp-build = "ociapp_build.cli:main"

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
