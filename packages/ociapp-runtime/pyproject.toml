[project]
name = "ociapp-runtime"
version = "0.1.0"
description = "Host-side OCIApp runtime"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ociapp",
]

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
